<h1>Nova Linha de Pagamento</h1>

<%= render 'form' %>

<div class="row">
  <div class="span6 offset3">
  <%= link_to 'Voltar Atrás', params[:order] ? "#{orders_path}/#{params[:order]}" : (@line_item.order_id ? "#{orders_path}/#{@line_item.order_id}" : line_items_path), class: "btn btn-group-sm btn-info" %>
  </div>
</div>
