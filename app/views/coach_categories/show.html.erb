<% provide(:title, "Categoria: #{@coach_category.name}" ) %>

<%= form_for(@coach_category, :html => { :class => "form-horizontal" }) do |f| %>
  <%= render 'shared/error_messages' %>

  <h1><%= @coach_category.name %></h1>

  <div class="row">
  <div class="span6 offset3">

    <%= f.label :order, "Ordem" %>
    <%= f.text_field :order, :disabled => 'disabled' %>

    <%= f.label :active, "Activa" %>
    <%= f.check_box :active, :disabled => 'disabled' %>

    <br/><br/>

    <%= link_to 'Editar', edit_coach_category_path(@coach_category) , class: "btn btn-group-sm btn-info" %>
    <%= link_to 'Voltar Atrás', coach_categories_path, class: "btn btn-group-sm btn-info" %>
    </div>
  </div>
<% end %>

